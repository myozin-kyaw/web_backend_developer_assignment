extend type Mutation {
    login(username: String!, password: String!): String!
}

extend type Query {
    me: User! @guard @auth
}

type User {
    id: ID!
    name: String!
    username: String!
    email: String!
    created_at: String!
    updated_at: String
}
